## Agent skills

### Issue tracker

이 저장소의 이슈와 명세는 GitHub Issues에서 관리한다. 자세한 운영 규칙은
`docs/agents/issue-tracker.md`를 참고한다.

### Triage labels

기본 triage 라벨 `needs-triage`, `needs-info`, `ready-for-agent`,
`ready-for-human`, `wontfix`를 사용한다. 자세한 매핑은
`docs/agents/triage-labels.md`를 참고한다.

### Domain docs

이 저장소는 루트 `CONTEXT.md`와 `docs/adr/`를 사용하는 single-context 구조다.
자세한 규칙은 `docs/agents/domain.md`를 참고한다.
